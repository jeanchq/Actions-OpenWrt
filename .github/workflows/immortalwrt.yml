name: Compile GL.iNet GL-MT6000 (2G+128G+MT7531AE)

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  compile-firmware:
    runs-on: ubuntu-22.04
    steps:
      # 1. 配置编译环境
      - name: Prepare build environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc
          sudo apt update -y && sudo apt full-upgrade -y
          sudo apt install -y build-essential clang flex bison g++ gawk gcc-multilib \
            gettext git libncurses5-dev libssl-dev python3-distutils rsync unzip zlib1g-dev \
            file wget curl libelf-dev libncursesw5-dev pkg-config python3-pip qemu-utils \
            python3-pyelftools swig subversion time upx-ucl uuid-dev
          sudo apt autoremove -y && sudo apt clean -y

      # 2. 克隆源码（适配 openwrt-24.10 分支）
      - name: Clone ImmortalWrt source code
        run: |
          git clone https://github.com/immortalwrt/immortalwrt.git immortalwrt
          cd immortalwrt
          # 强制切换到 openwrt-24.10（自动跟踪远程分支）
          git checkout -b openwrt-24.10 --track origin/openwrt-24.10
          git submodule update --init --recursive

      # 3. 核心修改：硬件适配（重构 sed 逻辑，避免正则错误）
      - name: Modify GL-MT6000 hardware config
        run: |
          cd immortalwrt
          # --------------------------
          # 3.1 适配 2GB 内存 + MT7531AE
          # --------------------------
          DTS_FILE=$(find target/linux/mediatek/dts -name "*gl-mt6000*.dts" | head -1)
          if [ -z "$DTS_FILE" ]; then
            DTS_FILE=$(find target/linux/mediatek/dts -name "*mt7986a-mt6000*.dts" | head -1)
          fi
          echo "Using DTS file: $DTS_FILE"
          
          # 安全修改内存配置（避免正则冲突）
          if grep -q "memory@40000000" $DTS_FILE; then
            sed -i '/memory@40000000/ {s/reg = <0x40000000 [0-9a-f]*>/reg = <0x40000000 0x80000000>/g;}' $DTS_FILE
          else
            # 若未找到内存节点，手动添加（兼容 openwrt-24.10）
            echo -e "\nmemory@40000000 { reg = <0x40000000 0x80000000>; };" >> $DTS_FILE
          fi
          
          # 安全修改 MT7531AE RGMII 模式
          if grep -q "phy-mode" $DTS_FILE; then
            sed -i '/phy-mode/ {s/phy-mode = .*/phy-mode = "rgmii";/g;}' $DTS_FILE
          else
            # 若未找到 phy-mode，在 gmac0 节点添加
            sed -i '/&gmac0/ a\    phy-mode = "rgmii";' $DTS_FILE
          fi

          # --------------------------
          # 3.2 自动检测分区表文件
          # --------------------------
          PARTITION_FILE=$(find target/linux/mediatek/image -name "*filogic*.mk" -o -name "*mt798*.mk" | head -1)
          if [ -z "$PARTITION_FILE" ]; then
            echo "Error: Partition file not found!"
            ls -l target/linux/mediatek/image/
            exit 1
          fi
          echo "Using partition file: $PARTITION_FILE"
          cp $PARTITION_FILE ${PARTITION_FILE}.bak  # 备份原文件

          # --------------------------
          # 3.3 适配 128G eMMC（安全追加，放弃 sed 替换）
          # --------------------------
          # 先删除旧的 GL-MT6000 配置（避免重复）
          sed -i '/define Device\/gl-mt6000/,/endef/ d' $PARTITION_FILE
          # 重新追加全新配置（彻底避免正则错误）
          cat >> $PARTITION_FILE << 'EOF'
          
          # GL-MT6000 (2G+128G eMMC+MT7531AE) Custom Config - openwrt-24.10
          define Device/gl-mt6000
            $(Device/uimage-factory)
            IMAGE_SIZE := 118000000k
            DEVICE_VENDOR := GL.iNet
            DEVICE_MODEL := GL-MT6000
            DEVICE_DTS := mt7986a-glinet-gl-mt6000
            DEVICE_ALT0_VENDOR := GL.iNet
            DEVICE_ALT0_MODEL := GL-MT6000
            DEVICE_ALT0_VARIANT := eMMC-128G
            DEVICE_PACKAGES := kmod-mt7986-firmware mt7986-firmware kmod-switch-mt7531 mt7531-firmware \
                     kmod-mmc kmod-sdhci-mt7622 luci luci-i18n-base-zh-cn
            BLOCKSIZE := 128k
            PAGESIZE := 2048
            SUBPAGESIZE := 2048
          endef
          TARGET_DEVICES += gl-mt6000
          EOF

      # 4. 加载配置（适配 openwrt-24.10）
      - name: Load GL-MT6000 defconfig
        run: |
          cd immortalwrt
          # 加载 filogic 通用配置（openwrt-24.10 主流）
          cp configs/templates/mediatek/filogic/config .config || cp configs/templates/mediatek/mt798x/config .config
          echo 'CONFIG_TARGET_DEVICE_mediatek_filogic_DEVICE_gl-mt6000=y' >> .config
          
          # 硬件适配配置（openwrt-24.10 兼容）
          echo 'CONFIG_TARGET_MEMORY_PROFILE_2048=y' >> .config
          echo 'CONFIG_LINUX_KERNEL_MEMORY_SIZE=2048' >> .config
          echo 'CONFIG_PACKAGE_kmod-switch-mt7531=y' >> .config
          echo 'CONFIG_PACKAGE_mt7531-firmware=y' >> .config
          echo '# CONFIG_PACKAGE_kmod-dsa-mt7530 is not set' >> .config
          echo 'CONFIG_SWITCH_MT7531_SPEED_GIGA=y' >> .config
          echo 'CONFIG_PACKAGE_kmod-mmc=y' >> .config
          echo 'CONFIG_PACKAGE_kmod-sdhci-mt7622=y' >> .config
          echo 'CONFIG_PACKAGE_luci-i18n-base-zh-cn=y' >> .config
          echo 'CONFIG_PACKAGE-ethtool=y' >> .config
          
          make defconfig

      # 5. 下载依赖
      - name: Download dependencies
        run: |
          cd immortalwrt
          make download -j$(nproc)
          find dl -size -1024c -exec rm -f {} \;

      # 6. 编译固件（openwrt-24.10 兼容）
      - name: Compile GL-MT6000 firmware
        run: |
          cd immortalwrt
          make -j$(($(nproc) + 1)) || make -j1 V=s
          # 兼容 openwrt-24.10 输出路径
          ls -lh bin/targets/mediatek/filogic/ | grep gl-mt6000 || ls -lh bin/targets/mediatek/mt798x/ | grep gl-mt6000

      # 7. 上传固件（适配 openwrt-24.10 路径）
      - name: Upload GL-MT6000 firmware
        uses: actions/upload-artifact@v4
        with:
          name: gl-mt6000-2g-128g-mt7531ae-firmware
          path: |
            immortalwrt/bin/targets/mediatek/filogic/*gl-mt6000*.bin
            immortalwrt/bin/targets/mediatek/filogic/*gl-mt6000*.img
            immortalwrt/bin/targets/mediatek/mt798x/*gl-mt6000*.bin
            immortalwrt/bin/targets/mediatek/mt798x/*gl-mt6000*.img
          retention-days: 30

      # 8. 上传到 Release
      - name: Create Release for GL-MT6000
        if: success()
        uses: softprops/action-gh-release@v2
        with:
          tag_name: gl-mt6000-2g-128g-mt7531ae-${{ github.run_id }}
          name: GL-MT6000 (2G+128G+MT7531AE) ImmortalWrt 24.10 ${{ github.run_id }}
          files: |
            immortalwrt/bin/targets/mediatek/filogic/*gl-mt6000*
            immortalwrt/bin/targets/mediatek/mt798x/*gl-mt6000*
          body: |
            ImmortalWrt Firmware for GL.iNet GL-MT6000 (openwrt-24.10)
            - Hardware: 2GB RAM + 128GB eMMC + MT7531AE Switch
            - Compile time: ${{ github.run_at }}
            - Note: Only for GL.iNet GL-MT6000 (NOT Xiaomi MT6000)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
